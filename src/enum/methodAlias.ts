import Level from './level';

const methodAlias = {
  fatal: Level.FATAL,
  error: Level.ERROR,
  warn: Level.WARN,
  info: Level.INFO,
  debug: Level.DEBUG,
  log: Level.INFO,
};

export default methodAlias;
